services:
  jimbleai:
    image: codeberg.org/henior/jimble-ai:latest
    build:
      context: .
      dockerfile: Dockerfile
      platforms:
        - "linux/amd64"
        - "linux/arm64"
    restart: "no"
